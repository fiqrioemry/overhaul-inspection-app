please refer to file inside folder /server named todo.md. understand the task given to you for improvement in client side folder. the todo.md is already executed to generate code in server side folder.

there's an updated new features and improvement from features existing.

for the coding guidance refer to /client/CLAUDE.md and always update changelog.md

- integrate the P0 - User bocking feature onto /client/src. add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate the PO - Two-Factor Authentication 2FA into /features/settings.add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate P1 - Hastags & Post Discovery feature (note :i dont know if this still in /features/post or need to separate it. you decide oke). add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate P1 - Post Mentions feature. add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate P1 - User Muting feature into /features/settings. add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate P1 - Message Reply-to feature into /features/chat. add the ui ux along with the api endpoints. query function and others related component that need to updated or added.

- integrate P1 - Profile Website Link into /features/settings. only need to add additional input for website field. make sure to update the schema and types.

make sure to understand the response types of related API endpoints and make the same variable attribute naming to type in /client/src project

NOTE : Skip P0 - Admin Panel API (this will execute in the next chat sessions)
